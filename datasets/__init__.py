"""
Dataset configs module.

Each file (bindingdb.py, humans.py, etc.) defines a config class
that handles data loading for that specific dataset.

All datasets must export a 'config' object with a load_data() method.
"""
